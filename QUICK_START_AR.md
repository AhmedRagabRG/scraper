# 🗺️ دليل الاستخدام السريع - Google Maps Scraper

## ✅ المتطلبات (تم تثبيتها)
- ✓ Python 3.13
- ✓ Playwright
- ✓ Pandas
- ✓ Chromium Browser

## 🚀 طريقة الاستخدام

### 1. تشغيل الـ Scraper

```bash
python3 main.py --query "نوع الأعمال في المدينة"
```

### 2. أمثلة عملية

**وكالات عقارية في القاهرة:**
```bash
python3 main.py --query "Real Estate Agencies in Cairo"
```

**مطاعم في الإسكندرية:**
```bash
python3 main.py --query "Restaurants in Alexandria"
```

**صيدليات في الجيزة:**
```bash
python3 main.py --query "Pharmacies in Giza"
```

**محامين في القاهرة:**
```bash
python3 main.py --query "Lawyers in Cairo"
```

### 3. خيارات إضافية

**تحديد مكان حفظ الملف:**
```bash
python3 main.py --query "Coffee Shops in Cairo" --output my_data.csv
```

**تشغيل مع عرض المتصفح (للمراقبة):**
```bash
python3 main.py --query "Hotels in Cairo" --no-headless
```

## 📊 البيانات المستخرجة

يقوم الـ scraper بجمع المعلومات التالية لكل business:

| الحقل | الوصف |
|------|------|
| business_name | اسم الشركة/المحل |
| rating | التقييم (من 5) |
| review_count | عدد المراجعات |
| phone | رقم التليفون |
| website | الموقع الإلكتروني |
| address | العنوان الكامل |

## 📁 مكان الملفات

- **النتائج:** تُحفظ في مجلد `output/`
- **اسم الملف:** `results_YYYYMMDD_HHMMSS.csv`

## 💡 نصائح

1. **استخدم استعلامات محددة:** بدلاً من "مطاعم"، استخدم "مطاعم في مدينة نصر، القاهرة"
2. **كن صبوراً:** جمع 100+ business قد يستغرق 5-10 دقائق
3. **افتح ملف CSV في Excel:** لعرض البيانات بشكل منظم

## ⚙️ حل المشاكل

### المشكلة: لا توجد نتائج
**الحل:** تأكد من كتابة الاستعلام بالإنجليزية وبشكل دقيق

### المشكلة: بطء في التنفيذ
**الحل:** هذا طبيعي - الـ scraper يفتح كل بطاقة لجمع التفاصيل

### المشكلة: بعض الحقول فارغة
**الحل:** بعض الأعمال لا تضع معلومات كاملة على Google Maps

## 🔄 تحديث المكتبات

```bash
pip3 install --upgrade -r requirements.txt
python3 -m playwright install chromium
```

## 📞 الدعم

إذا واجهت أي مشكلة:
1. تحقق من رسائل الخطأ في Terminal
2. ابحث عن ملفات screenshots في حالة حدوث أخطاء
3. جرب تشغيل الـ scraper مع `--no-headless` لمشاهدة ما يحدث

---

**تم الإنشاء بنجاح ✅**
